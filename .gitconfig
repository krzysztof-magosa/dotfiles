[alias]
	pr = pull --rebase
	tags = tag -l
	branches = branch -a
	remotes = remote -v

[core]
	excludesfile = ~/.gitexclude
	editor = emacs -nw

[push]
	default = current
	recurseSubmodules = on-demand

[include]
	path = ~/.gitconfig-local
	